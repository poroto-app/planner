// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package generated

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PlacePhotoVersion is an object representing the database table.
type PlacePhotoVersion struct {
	ID           string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	PlacePhotoID string    `boil:"place_photo_id" json:"place_photo_id" toml:"place_photo_id" yaml:"place_photo_id"`
	PhotoURL     string    `boil:"photo_url" json:"photo_url" toml:"photo_url" yaml:"photo_url"`
	Width        int       `boil:"width" json:"width" toml:"width" yaml:"width"`
	Height       int       `boil:"height" json:"height" toml:"height" yaml:"height"`
	CreatedAt    time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *placePhotoVersionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L placePhotoVersionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PlacePhotoVersionColumns = struct {
	ID           string
	PlacePhotoID string
	PhotoURL     string
	Width        string
	Height       string
	CreatedAt    string
	UpdatedAt    string
}{
	ID:           "id",
	PlacePhotoID: "place_photo_id",
	PhotoURL:     "photo_url",
	Width:        "width",
	Height:       "height",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
}

var PlacePhotoVersionTableColumns = struct {
	ID           string
	PlacePhotoID string
	PhotoURL     string
	Width        string
	Height       string
	CreatedAt    string
	UpdatedAt    string
}{
	ID:           "place_photo_versions.id",
	PlacePhotoID: "place_photo_versions.place_photo_id",
	PhotoURL:     "place_photo_versions.photo_url",
	Width:        "place_photo_versions.width",
	Height:       "place_photo_versions.height",
	CreatedAt:    "place_photo_versions.created_at",
	UpdatedAt:    "place_photo_versions.updated_at",
}

// Generated where

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var PlacePhotoVersionWhere = struct {
	ID           whereHelperstring
	PlacePhotoID whereHelperstring
	PhotoURL     whereHelperstring
	Width        whereHelperint
	Height       whereHelperint
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
}{
	ID:           whereHelperstring{field: "`place_photo_versions`.`id`"},
	PlacePhotoID: whereHelperstring{field: "`place_photo_versions`.`place_photo_id`"},
	PhotoURL:     whereHelperstring{field: "`place_photo_versions`.`photo_url`"},
	Width:        whereHelperint{field: "`place_photo_versions`.`width`"},
	Height:       whereHelperint{field: "`place_photo_versions`.`height`"},
	CreatedAt:    whereHelpertime_Time{field: "`place_photo_versions`.`created_at`"},
	UpdatedAt:    whereHelpertime_Time{field: "`place_photo_versions`.`updated_at`"},
}

// PlacePhotoVersionRels is where relationship names are stored.
var PlacePhotoVersionRels = struct {
	PlacePhoto string
}{
	PlacePhoto: "PlacePhoto",
}

// placePhotoVersionR is where relationships are stored.
type placePhotoVersionR struct {
	PlacePhoto *PlacePhoto `boil:"PlacePhoto" json:"PlacePhoto" toml:"PlacePhoto" yaml:"PlacePhoto"`
}

// NewStruct creates a new relationship struct
func (*placePhotoVersionR) NewStruct() *placePhotoVersionR {
	return &placePhotoVersionR{}
}

func (r *placePhotoVersionR) GetPlacePhoto() *PlacePhoto {
	if r == nil {
		return nil
	}
	return r.PlacePhoto
}

// placePhotoVersionL is where Load methods for each relationship are stored.
type placePhotoVersionL struct{}

var (
	placePhotoVersionAllColumns            = []string{"id", "place_photo_id", "photo_url", "width", "height", "created_at", "updated_at"}
	placePhotoVersionColumnsWithoutDefault = []string{"id", "place_photo_id", "photo_url", "width", "height"}
	placePhotoVersionColumnsWithDefault    = []string{"created_at", "updated_at"}
	placePhotoVersionPrimaryKeyColumns     = []string{"id"}
	placePhotoVersionGeneratedColumns      = []string{}
)

type (
	// PlacePhotoVersionSlice is an alias for a slice of pointers to PlacePhotoVersion.
	// This should almost always be used instead of []PlacePhotoVersion.
	PlacePhotoVersionSlice []*PlacePhotoVersion
	// PlacePhotoVersionHook is the signature for custom PlacePhotoVersion hook methods
	PlacePhotoVersionHook func(context.Context, boil.ContextExecutor, *PlacePhotoVersion) error

	placePhotoVersionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	placePhotoVersionType                 = reflect.TypeOf(&PlacePhotoVersion{})
	placePhotoVersionMapping              = queries.MakeStructMapping(placePhotoVersionType)
	placePhotoVersionPrimaryKeyMapping, _ = queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, placePhotoVersionPrimaryKeyColumns)
	placePhotoVersionInsertCacheMut       sync.RWMutex
	placePhotoVersionInsertCache          = make(map[string]insertCache)
	placePhotoVersionUpdateCacheMut       sync.RWMutex
	placePhotoVersionUpdateCache          = make(map[string]updateCache)
	placePhotoVersionUpsertCacheMut       sync.RWMutex
	placePhotoVersionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var placePhotoVersionAfterSelectMu sync.Mutex
var placePhotoVersionAfterSelectHooks []PlacePhotoVersionHook

var placePhotoVersionBeforeInsertMu sync.Mutex
var placePhotoVersionBeforeInsertHooks []PlacePhotoVersionHook
var placePhotoVersionAfterInsertMu sync.Mutex
var placePhotoVersionAfterInsertHooks []PlacePhotoVersionHook

var placePhotoVersionBeforeUpdateMu sync.Mutex
var placePhotoVersionBeforeUpdateHooks []PlacePhotoVersionHook
var placePhotoVersionAfterUpdateMu sync.Mutex
var placePhotoVersionAfterUpdateHooks []PlacePhotoVersionHook

var placePhotoVersionBeforeDeleteMu sync.Mutex
var placePhotoVersionBeforeDeleteHooks []PlacePhotoVersionHook
var placePhotoVersionAfterDeleteMu sync.Mutex
var placePhotoVersionAfterDeleteHooks []PlacePhotoVersionHook

var placePhotoVersionBeforeUpsertMu sync.Mutex
var placePhotoVersionBeforeUpsertHooks []PlacePhotoVersionHook
var placePhotoVersionAfterUpsertMu sync.Mutex
var placePhotoVersionAfterUpsertHooks []PlacePhotoVersionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PlacePhotoVersion) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PlacePhotoVersion) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PlacePhotoVersion) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PlacePhotoVersion) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PlacePhotoVersion) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PlacePhotoVersion) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PlacePhotoVersion) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PlacePhotoVersion) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PlacePhotoVersion) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range placePhotoVersionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPlacePhotoVersionHook registers your hook function for all future operations.
func AddPlacePhotoVersionHook(hookPoint boil.HookPoint, placePhotoVersionHook PlacePhotoVersionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		placePhotoVersionAfterSelectMu.Lock()
		placePhotoVersionAfterSelectHooks = append(placePhotoVersionAfterSelectHooks, placePhotoVersionHook)
		placePhotoVersionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		placePhotoVersionBeforeInsertMu.Lock()
		placePhotoVersionBeforeInsertHooks = append(placePhotoVersionBeforeInsertHooks, placePhotoVersionHook)
		placePhotoVersionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		placePhotoVersionAfterInsertMu.Lock()
		placePhotoVersionAfterInsertHooks = append(placePhotoVersionAfterInsertHooks, placePhotoVersionHook)
		placePhotoVersionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		placePhotoVersionBeforeUpdateMu.Lock()
		placePhotoVersionBeforeUpdateHooks = append(placePhotoVersionBeforeUpdateHooks, placePhotoVersionHook)
		placePhotoVersionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		placePhotoVersionAfterUpdateMu.Lock()
		placePhotoVersionAfterUpdateHooks = append(placePhotoVersionAfterUpdateHooks, placePhotoVersionHook)
		placePhotoVersionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		placePhotoVersionBeforeDeleteMu.Lock()
		placePhotoVersionBeforeDeleteHooks = append(placePhotoVersionBeforeDeleteHooks, placePhotoVersionHook)
		placePhotoVersionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		placePhotoVersionAfterDeleteMu.Lock()
		placePhotoVersionAfterDeleteHooks = append(placePhotoVersionAfterDeleteHooks, placePhotoVersionHook)
		placePhotoVersionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		placePhotoVersionBeforeUpsertMu.Lock()
		placePhotoVersionBeforeUpsertHooks = append(placePhotoVersionBeforeUpsertHooks, placePhotoVersionHook)
		placePhotoVersionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		placePhotoVersionAfterUpsertMu.Lock()
		placePhotoVersionAfterUpsertHooks = append(placePhotoVersionAfterUpsertHooks, placePhotoVersionHook)
		placePhotoVersionAfterUpsertMu.Unlock()
	}
}

// One returns a single placePhotoVersion record from the query.
func (q placePhotoVersionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PlacePhotoVersion, error) {
	o := &PlacePhotoVersion{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "generated: failed to execute a one query for place_photo_versions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PlacePhotoVersion records from the query.
func (q placePhotoVersionQuery) All(ctx context.Context, exec boil.ContextExecutor) (PlacePhotoVersionSlice, error) {
	var o []*PlacePhotoVersion

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "generated: failed to assign all query results to PlacePhotoVersion slice")
	}

	if len(placePhotoVersionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PlacePhotoVersion records in the query.
func (q placePhotoVersionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "generated: failed to count place_photo_versions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q placePhotoVersionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "generated: failed to check if place_photo_versions exists")
	}

	return count > 0, nil
}

// PlacePhoto pointed to by the foreign key.
func (o *PlacePhotoVersion) PlacePhoto(mods ...qm.QueryMod) placePhotoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.PlacePhotoID),
	}

	queryMods = append(queryMods, mods...)

	return PlacePhotos(queryMods...)
}

// LoadPlacePhoto allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (placePhotoVersionL) LoadPlacePhoto(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlacePhotoVersion interface{}, mods queries.Applicator) error {
	var slice []*PlacePhotoVersion
	var object *PlacePhotoVersion

	if singular {
		var ok bool
		object, ok = maybePlacePhotoVersion.(*PlacePhotoVersion)
		if !ok {
			object = new(PlacePhotoVersion)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlacePhotoVersion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlacePhotoVersion))
			}
		}
	} else {
		s, ok := maybePlacePhotoVersion.(*[]*PlacePhotoVersion)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlacePhotoVersion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlacePhotoVersion))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &placePhotoVersionR{}
		}
		args[object.PlacePhotoID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &placePhotoVersionR{}
			}

			args[obj.PlacePhotoID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`place_photos`),
		qm.WhereIn(`place_photos.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PlacePhoto")
	}

	var resultSlice []*PlacePhoto
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PlacePhoto")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for place_photos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for place_photos")
	}

	if len(placePhotoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PlacePhoto = foreign
		if foreign.R == nil {
			foreign.R = &placePhotoR{}
		}
		foreign.R.PlacePhotoVersions = append(foreign.R.PlacePhotoVersions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlacePhotoID == foreign.ID {
				local.R.PlacePhoto = foreign
				if foreign.R == nil {
					foreign.R = &placePhotoR{}
				}
				foreign.R.PlacePhotoVersions = append(foreign.R.PlacePhotoVersions, local)
				break
			}
		}
	}

	return nil
}

// SetPlacePhoto of the placePhotoVersion to the related item.
// Sets o.R.PlacePhoto to related.
// Adds o to related.R.PlacePhotoVersions.
func (o *PlacePhotoVersion) SetPlacePhoto(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PlacePhoto) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `place_photo_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"place_photo_id"}),
		strmangle.WhereClause("`", "`", 0, placePhotoVersionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PlacePhotoID = related.ID
	if o.R == nil {
		o.R = &placePhotoVersionR{
			PlacePhoto: related,
		}
	} else {
		o.R.PlacePhoto = related
	}

	if related.R == nil {
		related.R = &placePhotoR{
			PlacePhotoVersions: PlacePhotoVersionSlice{o},
		}
	} else {
		related.R.PlacePhotoVersions = append(related.R.PlacePhotoVersions, o)
	}

	return nil
}

// PlacePhotoVersions retrieves all the records using an executor.
func PlacePhotoVersions(mods ...qm.QueryMod) placePhotoVersionQuery {
	mods = append(mods, qm.From("`place_photo_versions`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`place_photo_versions`.*"})
	}

	return placePhotoVersionQuery{q}
}

// FindPlacePhotoVersion retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPlacePhotoVersion(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*PlacePhotoVersion, error) {
	placePhotoVersionObj := &PlacePhotoVersion{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `place_photo_versions` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, placePhotoVersionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "generated: unable to select from place_photo_versions")
	}

	if err = placePhotoVersionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return placePhotoVersionObj, err
	}

	return placePhotoVersionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PlacePhotoVersion) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("generated: no place_photo_versions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(placePhotoVersionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	placePhotoVersionInsertCacheMut.RLock()
	cache, cached := placePhotoVersionInsertCache[key]
	placePhotoVersionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			placePhotoVersionAllColumns,
			placePhotoVersionColumnsWithDefault,
			placePhotoVersionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `place_photo_versions` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `place_photo_versions` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `place_photo_versions` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, placePhotoVersionPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "generated: unable to insert into place_photo_versions")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "generated: unable to populate default values for place_photo_versions")
	}

CacheNoHooks:
	if !cached {
		placePhotoVersionInsertCacheMut.Lock()
		placePhotoVersionInsertCache[key] = cache
		placePhotoVersionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PlacePhotoVersion.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PlacePhotoVersion) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	placePhotoVersionUpdateCacheMut.RLock()
	cache, cached := placePhotoVersionUpdateCache[key]
	placePhotoVersionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			placePhotoVersionAllColumns,
			placePhotoVersionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("generated: unable to update place_photo_versions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `place_photo_versions` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, placePhotoVersionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, append(wl, placePhotoVersionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to update place_photo_versions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: failed to get rows affected by update for place_photo_versions")
	}

	if !cached {
		placePhotoVersionUpdateCacheMut.Lock()
		placePhotoVersionUpdateCache[key] = cache
		placePhotoVersionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q placePhotoVersionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to update all for place_photo_versions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to retrieve rows affected for place_photo_versions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PlacePhotoVersionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("generated: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), placePhotoVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `place_photo_versions` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, placePhotoVersionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to update all in placePhotoVersion slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to retrieve rows affected all in update all placePhotoVersion")
	}
	return rowsAff, nil
}

var mySQLPlacePhotoVersionUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PlacePhotoVersion) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("generated: no place_photo_versions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(placePhotoVersionColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLPlacePhotoVersionUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	placePhotoVersionUpsertCacheMut.RLock()
	cache, cached := placePhotoVersionUpsertCache[key]
	placePhotoVersionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			placePhotoVersionAllColumns,
			placePhotoVersionColumnsWithDefault,
			placePhotoVersionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			placePhotoVersionAllColumns,
			placePhotoVersionPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("generated: unable to upsert place_photo_versions, could not build update column list")
		}

		ret := strmangle.SetComplement(placePhotoVersionAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`place_photo_versions`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `place_photo_versions` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "generated: unable to upsert for place_photo_versions")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "generated: unable to retrieve unique values for place_photo_versions")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "generated: unable to populate default values for place_photo_versions")
	}

CacheNoHooks:
	if !cached {
		placePhotoVersionUpsertCacheMut.Lock()
		placePhotoVersionUpsertCache[key] = cache
		placePhotoVersionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PlacePhotoVersion record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PlacePhotoVersion) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("generated: no PlacePhotoVersion provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), placePhotoVersionPrimaryKeyMapping)
	sql := "DELETE FROM `place_photo_versions` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to delete from place_photo_versions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: failed to get rows affected by delete for place_photo_versions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q placePhotoVersionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("generated: no placePhotoVersionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to delete all from place_photo_versions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: failed to get rows affected by deleteall for place_photo_versions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PlacePhotoVersionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(placePhotoVersionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), placePhotoVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `place_photo_versions` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, placePhotoVersionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to delete all from placePhotoVersion slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: failed to get rows affected by deleteall for place_photo_versions")
	}

	if len(placePhotoVersionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PlacePhotoVersion) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPlacePhotoVersion(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlacePhotoVersionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PlacePhotoVersionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), placePhotoVersionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `place_photo_versions`.* FROM `place_photo_versions` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, placePhotoVersionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "generated: unable to reload all in PlacePhotoVersionSlice")
	}

	*o = slice

	return nil
}

// PlacePhotoVersionExists checks if the PlacePhotoVersion row exists.
func PlacePhotoVersionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `place_photo_versions` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "generated: unable to check if place_photo_versions exists")
	}

	return exists, nil
}

// Exists checks if the PlacePhotoVersion row exists.
func (o *PlacePhotoVersion) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PlacePhotoVersionExists(ctx, exec, o.ID)
}

// /////////////////////////////// BEGIN EXTENSIONS /////////////////////////////////
// Expose table columns
var (
	PlacePhotoVersionAllColumns            = placePhotoVersionAllColumns
	PlacePhotoVersionColumnsWithoutDefault = placePhotoVersionColumnsWithoutDefault
	PlacePhotoVersionColumnsWithDefault    = placePhotoVersionColumnsWithDefault
	PlacePhotoVersionPrimaryKeyColumns     = placePhotoVersionPrimaryKeyColumns
	PlacePhotoVersionGeneratedColumns      = placePhotoVersionGeneratedColumns
)

// GetID get ID from model object
func (o *PlacePhotoVersion) GetID() string {
	return o.ID
}

// GetIDs extract IDs from model objects
func (s PlacePhotoVersionSlice) GetIDs() []string {
	result := make([]string, len(s))
	for i := range s {
		result[i] = s[i].ID
	}
	return result
}

// GetIntfIDs extract IDs from model objects as interface slice
func (s PlacePhotoVersionSlice) GetIntfIDs() []interface{} {
	result := make([]interface{}, len(s))
	for i := range s {
		result[i] = s[i].ID
	}
	return result
}

// ToIDMap convert a slice of model objects to a map with ID as key
func (s PlacePhotoVersionSlice) ToIDMap() map[string]*PlacePhotoVersion {
	result := make(map[string]*PlacePhotoVersion, len(s))
	for _, o := range s {
		result[o.ID] = o
	}
	return result
}

// ToUniqueItems construct a slice of unique items from the given slice
func (s PlacePhotoVersionSlice) ToUniqueItems() PlacePhotoVersionSlice {
	result := make(PlacePhotoVersionSlice, 0, len(s))
	mapChk := make(map[string]struct{}, len(s))
	for i := len(s) - 1; i >= 0; i-- {
		o := s[i]
		if _, ok := mapChk[o.ID]; !ok {
			mapChk[o.ID] = struct{}{}
			result = append(result, o)
		}
	}
	return result
}

// FindItemByID find item by ID in the slice
func (s PlacePhotoVersionSlice) FindItemByID(id string) *PlacePhotoVersion {
	for _, o := range s {
		if o.ID == id {
			return o
		}
	}
	return nil
}

// FindMissingItemIDs find all item IDs that are not in the list
// NOTE: the input ID slice should contain unique values
func (s PlacePhotoVersionSlice) FindMissingItemIDs(expectedIDs []string) []string {
	if len(s) == 0 {
		return expectedIDs
	}
	result := []string{}
	mapChk := s.ToIDMap()
	for _, id := range expectedIDs {
		if _, ok := mapChk[id]; !ok {
			result = append(result, id)
		}
	}
	return result
}

// InsertAll inserts all rows with the specified column values, using an executor.
func (o PlacePhotoVersionSlice) InsertAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var sql string
	vals := []interface{}{}
	for i, row := range o {
		if !boil.TimestampsAreSkipped(ctx) {
			currTime := time.Now().In(boil.GetLocation())
			if row.CreatedAt.IsZero() {
				row.CreatedAt = currTime
			}
			if row.UpdatedAt.IsZero() {
				row.UpdatedAt = currTime
			}
		}

		if err := row.doBeforeInsertHooks(ctx, exec); err != nil {
			return 0, err
		}

		wl, _ := columns.InsertColumnSet(
			placePhotoVersionAllColumns,
			placePhotoVersionColumnsWithDefault,
			placePhotoVersionColumnsWithoutDefault,
			queries.NonZeroDefaultSet(placePhotoVersionColumnsWithDefault, row),
		)
		if i == 0 {
			sql = "INSERT INTO `place_photo_versions` " + "(`" + strings.Join(wl, "`,`") + "`)" + " VALUES "
		}
		sql += strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), len(vals)+1, len(wl))
		if i != len(o)-1 {
			sql += ","
		}
		valMapping, err := queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, wl)
		if err != nil {
			return 0, err
		}

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, sql, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to insert all from placePhotoVersion slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: failed to get rows affected by insertall for place_photo_versions")
	}

	if len(placePhotoVersionAfterInsertHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterInsertHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// UpsertAll inserts or updates all rows
// Currently it doesn't support "NoContext" and "NoRowsAffected"
func (o PlacePhotoVersionSlice) UpsertAll(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	nzDefaults := queries.NonZeroDefaultSet(placePhotoVersionColumnsWithDefault, o[0])
	nzUniques := queries.NonZeroDefaultSet(mySQLPlacePhotoVersionUniqueColumns, o[0])
	if len(nzUniques) == 0 {
		return 0, errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	insert, _ := insertColumns.InsertColumnSet(
		placePhotoVersionAllColumns,
		placePhotoVersionColumnsWithDefault,
		placePhotoVersionColumnsWithoutDefault,
		nzDefaults,
	)
	update := updateColumns.UpdateColumnSet(
		placePhotoVersionAllColumns,
		placePhotoVersionPrimaryKeyColumns,
	)
	if !updateColumns.IsNone() && len(update) == 0 {
		return 0, errors.New("generated: unable to upsert place_photo_versions, could not build update column list")
	}

	buf := strmangle.GetBuffer()
	defer strmangle.PutBuffer(buf)

	if len(update) == 0 {
		fmt.Fprintf(
			buf,
			"INSERT IGNORE INTO `place_photo_versions`(%s) VALUES %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, insert), ","),
			strmangle.Placeholders(false, len(insert)*len(o), 1, len(insert)),
		)
	} else {
		fmt.Fprintf(
			buf,
			"INSERT INTO `place_photo_versions`(%s) VALUES %s ON DUPLICATE KEY UPDATE ",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, insert), ","),
			strmangle.Placeholders(false, len(insert)*len(o), 1, len(insert)),
		)

		for i, v := range update {
			if i != 0 {
				buf.WriteByte(',')
			}
			quoted := strmangle.IdentQuote(dialect.LQ, dialect.RQ, v)
			buf.WriteString(quoted)
			buf.WriteString(" = VALUES(")
			buf.WriteString(quoted)
			buf.WriteByte(')')
		}
	}

	query := buf.String()
	valueMapping, err := queries.BindMapping(placePhotoVersionType, placePhotoVersionMapping, insert)
	if err != nil {
		return 0, err
	}

	var vals []interface{}
	for _, row := range o {
		if !boil.TimestampsAreSkipped(ctx) {
			currTime := time.Now().In(boil.GetLocation())
			if row.CreatedAt.IsZero() {
				row.CreatedAt = currTime
			}

			row.UpdatedAt = currTime
		}

		if err := row.doBeforeUpsertHooks(ctx, exec); err != nil {
			return 0, err
		}

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valueMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, query, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "generated: unable to upsert for place_photo_versions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "generated: failed to get rows affected by upsert for place_photo_versions")
	}

	if len(placePhotoVersionAfterUpsertHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterUpsertHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// DeleteAllByPage delete all PlacePhotoVersion records from the slice.
// This function deletes data by pages to avoid exceeding Mysql limitation (max placeholders: 65535)
// Mysql Error 1390: Prepared statement contains too many placeholders.
func (s PlacePhotoVersionSlice) DeleteAllByPage(ctx context.Context, exec boil.ContextExecutor, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// MySQL max placeholders = 65535
	chunkSize := DefaultPageSize
	if len(limits) > 0 && limits[0] > 0 && limits[0] <= MaxPageSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.DeleteAll(ctx, exec)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].DeleteAll(ctx, exec)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// UpdateAllByPage update all PlacePhotoVersion records from the slice.
// This function updates data by pages to avoid exceeding Mysql limitation (max placeholders: 65535)
// Mysql Error 1390: Prepared statement contains too many placeholders.
func (s PlacePhotoVersionSlice) UpdateAllByPage(ctx context.Context, exec boil.ContextExecutor, cols M, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// MySQL max placeholders = 65535
	// NOTE (eric): len(cols) should not be too big
	chunkSize := DefaultPageSize
	if len(limits) > 0 && limits[0] > 0 && limits[0] <= MaxPageSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.UpdateAll(ctx, exec, cols)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].UpdateAll(ctx, exec, cols)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// InsertAllByPage insert all PlacePhotoVersion records from the slice.
// This function inserts data by pages to avoid exceeding Mysql limitation (max placeholders: 65535)
// Mysql Error 1390: Prepared statement contains too many placeholders.
func (s PlacePhotoVersionSlice) InsertAllByPage(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// MySQL max placeholders = 65535
	chunkSize := MaxPageSize / reflect.ValueOf(&PlacePhotoVersionColumns).Elem().NumField()
	if len(limits) > 0 && limits[0] > 0 && limits[0] < chunkSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.InsertAll(ctx, exec, columns)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].InsertAll(ctx, exec, columns)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// UpsertAllByPage upsert all PlacePhotoVersion records from the slice.
// This function upserts data by pages to avoid exceeding Mysql limitation (max placeholders: 65535)
// Mysql Error 1390: Prepared statement contains too many placeholders.
func (s PlacePhotoVersionSlice) UpsertAllByPage(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// MySQL max placeholders = 65535
	chunkSize := MaxPageSize / reflect.ValueOf(&PlacePhotoVersionColumns).Elem().NumField()
	if len(limits) > 0 && limits[0] > 0 && limits[0] < chunkSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.UpsertAll(ctx, exec, updateColumns, insertColumns)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].UpsertAll(ctx, exec, updateColumns, insertColumns)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// LoadPlacePhotosByPage performs eager loading of values by page. This is for a N-1 relationship.
func (s PlacePhotoVersionSlice) LoadPlacePhotosByPage(ctx context.Context, e boil.ContextExecutor, mods ...qm.QueryMod) error {
	return s.LoadPlacePhotosByPageEx(ctx, e, DefaultPageSize, mods...)
}
func (s PlacePhotoVersionSlice) LoadPlacePhotosByPageEx(ctx context.Context, e boil.ContextExecutor, pageSize int, mods ...qm.QueryMod) error {
	if len(s) == 0 {
		return nil
	}
	for _, chunk := range chunkSlice[*PlacePhotoVersion](s, pageSize) {
		if err := chunk[0].L.LoadPlacePhoto(ctx, e, false, &chunk, queryMods(mods)); err != nil {
			return err
		}
	}
	return nil
}

func (s PlacePhotoVersionSlice) GetLoadedPlacePhotos() PlacePhotoSlice {
	result := make(PlacePhotoSlice, 0, len(s))
	mapCheckDup := make(map[*PlacePhoto]struct{})
	for _, item := range s {
		if item.R == nil || item.R.PlacePhoto == nil {
			continue
		}
		if _, ok := mapCheckDup[item.R.PlacePhoto]; ok {
			continue
		}
		result = append(result, item.R.PlacePhoto)
		mapCheckDup[item.R.PlacePhoto] = struct{}{}
	}
	return result
}

///////////////////////////////// END EXTENSIONS /////////////////////////////////
