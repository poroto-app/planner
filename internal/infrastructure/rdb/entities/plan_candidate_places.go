// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entities

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PlanCandidatePlace is an object representing the database table.
type PlanCandidatePlace struct {
	ID              string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	PlanCandidateID null.String `boil:"plan_candidate_id" json:"plan_candidate_id,omitempty" toml:"plan_candidate_id" yaml:"plan_candidate_id,omitempty"`
	PlaceID         null.String `boil:"place_id" json:"place_id,omitempty" toml:"place_id" yaml:"place_id,omitempty"`
	Order           null.Int    `boil:"order" json:"order,omitempty" toml:"order" yaml:"order,omitempty"`
	CreatedAt       null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt       null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *planCandidatePlaceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L planCandidatePlaceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PlanCandidatePlaceColumns = struct {
	ID              string
	PlanCandidateID string
	PlaceID         string
	Order           string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "id",
	PlanCandidateID: "plan_candidate_id",
	PlaceID:         "place_id",
	Order:           "order",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var PlanCandidatePlaceTableColumns = struct {
	ID              string
	PlanCandidateID string
	PlaceID         string
	Order           string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "plan_candidate_places.id",
	PlanCandidateID: "plan_candidate_places.plan_candidate_id",
	PlaceID:         "plan_candidate_places.place_id",
	Order:           "plan_candidate_places.order",
	CreatedAt:       "plan_candidate_places.created_at",
	UpdatedAt:       "plan_candidate_places.updated_at",
}

// Generated where

var PlanCandidatePlaceWhere = struct {
	ID              whereHelperstring
	PlanCandidateID whereHelpernull_String
	PlaceID         whereHelpernull_String
	Order           whereHelpernull_Int
	CreatedAt       whereHelpernull_Time
	UpdatedAt       whereHelpernull_Time
}{
	ID:              whereHelperstring{field: "`plan_candidate_places`.`id`"},
	PlanCandidateID: whereHelpernull_String{field: "`plan_candidate_places`.`plan_candidate_id`"},
	PlaceID:         whereHelpernull_String{field: "`plan_candidate_places`.`place_id`"},
	Order:           whereHelpernull_Int{field: "`plan_candidate_places`.`order`"},
	CreatedAt:       whereHelpernull_Time{field: "`plan_candidate_places`.`created_at`"},
	UpdatedAt:       whereHelpernull_Time{field: "`plan_candidate_places`.`updated_at`"},
}

// PlanCandidatePlaceRels is where relationship names are stored.
var PlanCandidatePlaceRels = struct {
	PlanCandidate string
	Place         string
}{
	PlanCandidate: "PlanCandidate",
	Place:         "Place",
}

// planCandidatePlaceR is where relationships are stored.
type planCandidatePlaceR struct {
	PlanCandidate *PlanCandidate `boil:"PlanCandidate" json:"PlanCandidate" toml:"PlanCandidate" yaml:"PlanCandidate"`
	Place         *Place         `boil:"Place" json:"Place" toml:"Place" yaml:"Place"`
}

// NewStruct creates a new relationship struct
func (*planCandidatePlaceR) NewStruct() *planCandidatePlaceR {
	return &planCandidatePlaceR{}
}

func (r *planCandidatePlaceR) GetPlanCandidate() *PlanCandidate {
	if r == nil {
		return nil
	}
	return r.PlanCandidate
}

func (r *planCandidatePlaceR) GetPlace() *Place {
	if r == nil {
		return nil
	}
	return r.Place
}

// planCandidatePlaceL is where Load methods for each relationship are stored.
type planCandidatePlaceL struct{}

var (
	planCandidatePlaceAllColumns            = []string{"id", "plan_candidate_id", "place_id", "order", "created_at", "updated_at"}
	planCandidatePlaceColumnsWithoutDefault = []string{"id", "plan_candidate_id", "place_id", "order"}
	planCandidatePlaceColumnsWithDefault    = []string{"created_at", "updated_at"}
	planCandidatePlacePrimaryKeyColumns     = []string{"id"}
	planCandidatePlaceGeneratedColumns      = []string{}
)

type (
	// PlanCandidatePlaceSlice is an alias for a slice of pointers to PlanCandidatePlace.
	// This should almost always be used instead of []PlanCandidatePlace.
	PlanCandidatePlaceSlice []*PlanCandidatePlace
	// PlanCandidatePlaceHook is the signature for custom PlanCandidatePlace hook methods
	PlanCandidatePlaceHook func(context.Context, boil.ContextExecutor, *PlanCandidatePlace) error

	planCandidatePlaceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	planCandidatePlaceType                 = reflect.TypeOf(&PlanCandidatePlace{})
	planCandidatePlaceMapping              = queries.MakeStructMapping(planCandidatePlaceType)
	planCandidatePlacePrimaryKeyMapping, _ = queries.BindMapping(planCandidatePlaceType, planCandidatePlaceMapping, planCandidatePlacePrimaryKeyColumns)
	planCandidatePlaceInsertCacheMut       sync.RWMutex
	planCandidatePlaceInsertCache          = make(map[string]insertCache)
	planCandidatePlaceUpdateCacheMut       sync.RWMutex
	planCandidatePlaceUpdateCache          = make(map[string]updateCache)
	planCandidatePlaceUpsertCacheMut       sync.RWMutex
	planCandidatePlaceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var planCandidatePlaceAfterSelectHooks []PlanCandidatePlaceHook

var planCandidatePlaceBeforeInsertHooks []PlanCandidatePlaceHook
var planCandidatePlaceAfterInsertHooks []PlanCandidatePlaceHook

var planCandidatePlaceBeforeUpdateHooks []PlanCandidatePlaceHook
var planCandidatePlaceAfterUpdateHooks []PlanCandidatePlaceHook

var planCandidatePlaceBeforeDeleteHooks []PlanCandidatePlaceHook
var planCandidatePlaceAfterDeleteHooks []PlanCandidatePlaceHook

var planCandidatePlaceBeforeUpsertHooks []PlanCandidatePlaceHook
var planCandidatePlaceAfterUpsertHooks []PlanCandidatePlaceHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PlanCandidatePlace) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PlanCandidatePlace) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PlanCandidatePlace) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PlanCandidatePlace) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PlanCandidatePlace) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PlanCandidatePlace) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PlanCandidatePlace) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PlanCandidatePlace) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PlanCandidatePlace) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range planCandidatePlaceAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPlanCandidatePlaceHook registers your hook function for all future operations.
func AddPlanCandidatePlaceHook(hookPoint boil.HookPoint, planCandidatePlaceHook PlanCandidatePlaceHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		planCandidatePlaceAfterSelectHooks = append(planCandidatePlaceAfterSelectHooks, planCandidatePlaceHook)
	case boil.BeforeInsertHook:
		planCandidatePlaceBeforeInsertHooks = append(planCandidatePlaceBeforeInsertHooks, planCandidatePlaceHook)
	case boil.AfterInsertHook:
		planCandidatePlaceAfterInsertHooks = append(planCandidatePlaceAfterInsertHooks, planCandidatePlaceHook)
	case boil.BeforeUpdateHook:
		planCandidatePlaceBeforeUpdateHooks = append(planCandidatePlaceBeforeUpdateHooks, planCandidatePlaceHook)
	case boil.AfterUpdateHook:
		planCandidatePlaceAfterUpdateHooks = append(planCandidatePlaceAfterUpdateHooks, planCandidatePlaceHook)
	case boil.BeforeDeleteHook:
		planCandidatePlaceBeforeDeleteHooks = append(planCandidatePlaceBeforeDeleteHooks, planCandidatePlaceHook)
	case boil.AfterDeleteHook:
		planCandidatePlaceAfterDeleteHooks = append(planCandidatePlaceAfterDeleteHooks, planCandidatePlaceHook)
	case boil.BeforeUpsertHook:
		planCandidatePlaceBeforeUpsertHooks = append(planCandidatePlaceBeforeUpsertHooks, planCandidatePlaceHook)
	case boil.AfterUpsertHook:
		planCandidatePlaceAfterUpsertHooks = append(planCandidatePlaceAfterUpsertHooks, planCandidatePlaceHook)
	}
}

// One returns a single planCandidatePlace record from the query.
func (q planCandidatePlaceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PlanCandidatePlace, error) {
	o := &PlanCandidatePlace{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entities: failed to execute a one query for plan_candidate_places")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PlanCandidatePlace records from the query.
func (q planCandidatePlaceQuery) All(ctx context.Context, exec boil.ContextExecutor) (PlanCandidatePlaceSlice, error) {
	var o []*PlanCandidatePlace

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entities: failed to assign all query results to PlanCandidatePlace slice")
	}

	if len(planCandidatePlaceAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PlanCandidatePlace records in the query.
func (q planCandidatePlaceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entities: failed to count plan_candidate_places rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q planCandidatePlaceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entities: failed to check if plan_candidate_places exists")
	}

	return count > 0, nil
}

// PlanCandidate pointed to by the foreign key.
func (o *PlanCandidatePlace) PlanCandidate(mods ...qm.QueryMod) planCandidateQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.PlanCandidateID),
	}

	queryMods = append(queryMods, mods...)

	return PlanCandidates(queryMods...)
}

// Place pointed to by the foreign key.
func (o *PlanCandidatePlace) Place(mods ...qm.QueryMod) placeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.PlaceID),
	}

	queryMods = append(queryMods, mods...)

	return Places(queryMods...)
}

// LoadPlanCandidate allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (planCandidatePlaceL) LoadPlanCandidate(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlanCandidatePlace interface{}, mods queries.Applicator) error {
	var slice []*PlanCandidatePlace
	var object *PlanCandidatePlace

	if singular {
		var ok bool
		object, ok = maybePlanCandidatePlace.(*PlanCandidatePlace)
		if !ok {
			object = new(PlanCandidatePlace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlanCandidatePlace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlanCandidatePlace))
			}
		}
	} else {
		s, ok := maybePlanCandidatePlace.(*[]*PlanCandidatePlace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlanCandidatePlace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlanCandidatePlace))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &planCandidatePlaceR{}
		}
		if !queries.IsNil(object.PlanCandidateID) {
			args = append(args, object.PlanCandidateID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &planCandidatePlaceR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.PlanCandidateID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.PlanCandidateID) {
				args = append(args, obj.PlanCandidateID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`plan_candidates`),
		qm.WhereIn(`plan_candidates.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PlanCandidate")
	}

	var resultSlice []*PlanCandidate
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PlanCandidate")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for plan_candidates")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for plan_candidates")
	}

	if len(planCandidateAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PlanCandidate = foreign
		if foreign.R == nil {
			foreign.R = &planCandidateR{}
		}
		foreign.R.PlanCandidatePlaces = append(foreign.R.PlanCandidatePlaces, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.PlanCandidateID, foreign.ID) {
				local.R.PlanCandidate = foreign
				if foreign.R == nil {
					foreign.R = &planCandidateR{}
				}
				foreign.R.PlanCandidatePlaces = append(foreign.R.PlanCandidatePlaces, local)
				break
			}
		}
	}

	return nil
}

// LoadPlace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (planCandidatePlaceL) LoadPlace(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlanCandidatePlace interface{}, mods queries.Applicator) error {
	var slice []*PlanCandidatePlace
	var object *PlanCandidatePlace

	if singular {
		var ok bool
		object, ok = maybePlanCandidatePlace.(*PlanCandidatePlace)
		if !ok {
			object = new(PlanCandidatePlace)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlanCandidatePlace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlanCandidatePlace))
			}
		}
	} else {
		s, ok := maybePlanCandidatePlace.(*[]*PlanCandidatePlace)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlanCandidatePlace)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlanCandidatePlace))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &planCandidatePlaceR{}
		}
		if !queries.IsNil(object.PlaceID) {
			args = append(args, object.PlaceID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &planCandidatePlaceR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.PlaceID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.PlaceID) {
				args = append(args, obj.PlaceID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`places`),
		qm.WhereIn(`places.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Place")
	}

	var resultSlice []*Place
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Place")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for places")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for places")
	}

	if len(placeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Place = foreign
		if foreign.R == nil {
			foreign.R = &placeR{}
		}
		foreign.R.PlanCandidatePlaces = append(foreign.R.PlanCandidatePlaces, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.PlaceID, foreign.ID) {
				local.R.Place = foreign
				if foreign.R == nil {
					foreign.R = &placeR{}
				}
				foreign.R.PlanCandidatePlaces = append(foreign.R.PlanCandidatePlaces, local)
				break
			}
		}
	}

	return nil
}

// SetPlanCandidate of the planCandidatePlace to the related item.
// Sets o.R.PlanCandidate to related.
// Adds o to related.R.PlanCandidatePlaces.
func (o *PlanCandidatePlace) SetPlanCandidate(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PlanCandidate) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `plan_candidate_places` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"plan_candidate_id"}),
		strmangle.WhereClause("`", "`", 0, planCandidatePlacePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.PlanCandidateID, related.ID)
	if o.R == nil {
		o.R = &planCandidatePlaceR{
			PlanCandidate: related,
		}
	} else {
		o.R.PlanCandidate = related
	}

	if related.R == nil {
		related.R = &planCandidateR{
			PlanCandidatePlaces: PlanCandidatePlaceSlice{o},
		}
	} else {
		related.R.PlanCandidatePlaces = append(related.R.PlanCandidatePlaces, o)
	}

	return nil
}

// RemovePlanCandidate relationship.
// Sets o.R.PlanCandidate to nil.
// Removes o from all passed in related items' relationships struct.
func (o *PlanCandidatePlace) RemovePlanCandidate(ctx context.Context, exec boil.ContextExecutor, related *PlanCandidate) error {
	var err error

	queries.SetScanner(&o.PlanCandidateID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("plan_candidate_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.PlanCandidate = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.PlanCandidatePlaces {
		if queries.Equal(o.PlanCandidateID, ri.PlanCandidateID) {
			continue
		}

		ln := len(related.R.PlanCandidatePlaces)
		if ln > 1 && i < ln-1 {
			related.R.PlanCandidatePlaces[i] = related.R.PlanCandidatePlaces[ln-1]
		}
		related.R.PlanCandidatePlaces = related.R.PlanCandidatePlaces[:ln-1]
		break
	}
	return nil
}

// SetPlace of the planCandidatePlace to the related item.
// Sets o.R.Place to related.
// Adds o to related.R.PlanCandidatePlaces.
func (o *PlanCandidatePlace) SetPlace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Place) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `plan_candidate_places` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"place_id"}),
		strmangle.WhereClause("`", "`", 0, planCandidatePlacePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.PlaceID, related.ID)
	if o.R == nil {
		o.R = &planCandidatePlaceR{
			Place: related,
		}
	} else {
		o.R.Place = related
	}

	if related.R == nil {
		related.R = &placeR{
			PlanCandidatePlaces: PlanCandidatePlaceSlice{o},
		}
	} else {
		related.R.PlanCandidatePlaces = append(related.R.PlanCandidatePlaces, o)
	}

	return nil
}

// RemovePlace relationship.
// Sets o.R.Place to nil.
// Removes o from all passed in related items' relationships struct.
func (o *PlanCandidatePlace) RemovePlace(ctx context.Context, exec boil.ContextExecutor, related *Place) error {
	var err error

	queries.SetScanner(&o.PlaceID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("place_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Place = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.PlanCandidatePlaces {
		if queries.Equal(o.PlaceID, ri.PlaceID) {
			continue
		}

		ln := len(related.R.PlanCandidatePlaces)
		if ln > 1 && i < ln-1 {
			related.R.PlanCandidatePlaces[i] = related.R.PlanCandidatePlaces[ln-1]
		}
		related.R.PlanCandidatePlaces = related.R.PlanCandidatePlaces[:ln-1]
		break
	}
	return nil
}

// PlanCandidatePlaces retrieves all the records using an executor.
func PlanCandidatePlaces(mods ...qm.QueryMod) planCandidatePlaceQuery {
	mods = append(mods, qm.From("`plan_candidate_places`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`plan_candidate_places`.*"})
	}

	return planCandidatePlaceQuery{q}
}

// FindPlanCandidatePlace retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPlanCandidatePlace(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*PlanCandidatePlace, error) {
	planCandidatePlaceObj := &PlanCandidatePlace{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `plan_candidate_places` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, planCandidatePlaceObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entities: unable to select from plan_candidate_places")
	}

	if err = planCandidatePlaceObj.doAfterSelectHooks(ctx, exec); err != nil {
		return planCandidatePlaceObj, err
	}

	return planCandidatePlaceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PlanCandidatePlace) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entities: no plan_candidate_places provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(planCandidatePlaceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	planCandidatePlaceInsertCacheMut.RLock()
	cache, cached := planCandidatePlaceInsertCache[key]
	planCandidatePlaceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			planCandidatePlaceAllColumns,
			planCandidatePlaceColumnsWithDefault,
			planCandidatePlaceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(planCandidatePlaceType, planCandidatePlaceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(planCandidatePlaceType, planCandidatePlaceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `plan_candidate_places` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `plan_candidate_places` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `plan_candidate_places` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, planCandidatePlacePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "entities: unable to insert into plan_candidate_places")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "entities: unable to populate default values for plan_candidate_places")
	}

CacheNoHooks:
	if !cached {
		planCandidatePlaceInsertCacheMut.Lock()
		planCandidatePlaceInsertCache[key] = cache
		planCandidatePlaceInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PlanCandidatePlace.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PlanCandidatePlace) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	planCandidatePlaceUpdateCacheMut.RLock()
	cache, cached := planCandidatePlaceUpdateCache[key]
	planCandidatePlaceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			planCandidatePlaceAllColumns,
			planCandidatePlacePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entities: unable to update plan_candidate_places, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `plan_candidate_places` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, planCandidatePlacePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(planCandidatePlaceType, planCandidatePlaceMapping, append(wl, planCandidatePlacePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to update plan_candidate_places row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entities: failed to get rows affected by update for plan_candidate_places")
	}

	if !cached {
		planCandidatePlaceUpdateCacheMut.Lock()
		planCandidatePlaceUpdateCache[key] = cache
		planCandidatePlaceUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q planCandidatePlaceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to update all for plan_candidate_places")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to retrieve rows affected for plan_candidate_places")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PlanCandidatePlaceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entities: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), planCandidatePlacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `plan_candidate_places` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, planCandidatePlacePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to update all in planCandidatePlace slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to retrieve rows affected all in update all planCandidatePlace")
	}
	return rowsAff, nil
}

var mySQLPlanCandidatePlaceUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PlanCandidatePlace) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("entities: no plan_candidate_places provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(planCandidatePlaceColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLPlanCandidatePlaceUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	planCandidatePlaceUpsertCacheMut.RLock()
	cache, cached := planCandidatePlaceUpsertCache[key]
	planCandidatePlaceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			planCandidatePlaceAllColumns,
			planCandidatePlaceColumnsWithDefault,
			planCandidatePlaceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			planCandidatePlaceAllColumns,
			planCandidatePlacePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("entities: unable to upsert plan_candidate_places, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`plan_candidate_places`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `plan_candidate_places` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(planCandidatePlaceType, planCandidatePlaceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(planCandidatePlaceType, planCandidatePlaceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "entities: unable to upsert for plan_candidate_places")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(planCandidatePlaceType, planCandidatePlaceMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "entities: unable to retrieve unique values for plan_candidate_places")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "entities: unable to populate default values for plan_candidate_places")
	}

CacheNoHooks:
	if !cached {
		planCandidatePlaceUpsertCacheMut.Lock()
		planCandidatePlaceUpsertCache[key] = cache
		planCandidatePlaceUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PlanCandidatePlace record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PlanCandidatePlace) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("entities: no PlanCandidatePlace provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), planCandidatePlacePrimaryKeyMapping)
	sql := "DELETE FROM `plan_candidate_places` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to delete from plan_candidate_places")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entities: failed to get rows affected by delete for plan_candidate_places")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q planCandidatePlaceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entities: no planCandidatePlaceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to delete all from plan_candidate_places")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entities: failed to get rows affected by deleteall for plan_candidate_places")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PlanCandidatePlaceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(planCandidatePlaceBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), planCandidatePlacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `plan_candidate_places` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, planCandidatePlacePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entities: unable to delete all from planCandidatePlace slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entities: failed to get rows affected by deleteall for plan_candidate_places")
	}

	if len(planCandidatePlaceAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PlanCandidatePlace) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPlanCandidatePlace(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlanCandidatePlaceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PlanCandidatePlaceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), planCandidatePlacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `plan_candidate_places`.* FROM `plan_candidate_places` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, planCandidatePlacePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entities: unable to reload all in PlanCandidatePlaceSlice")
	}

	*o = slice

	return nil
}

// PlanCandidatePlaceExists checks if the PlanCandidatePlace row exists.
func PlanCandidatePlaceExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `plan_candidate_places` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entities: unable to check if plan_candidate_places exists")
	}

	return exists, nil
}

// Exists checks if the PlanCandidatePlace row exists.
func (o *PlanCandidatePlace) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PlanCandidatePlaceExists(ctx, exec, o.ID)
}
